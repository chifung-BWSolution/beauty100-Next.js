'use client';

import ZoneTopBar from '@/components/public/ZoneTopBar';

/** Main-site top bar — same zone switcher so users can enter KOL / merchant */
export default function PublicTopBar() {
  return <ZoneTopBar />;
}
